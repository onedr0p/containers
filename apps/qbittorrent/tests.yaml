schemaVersion: "2.0.0"
commandTests:
  - name: test
    command: timeout
    args:
      - --preserve-status
      - 30s
      - /entrypoint.sh
    expectedOutput:
      - .*Now listening on IP.*8080.*
fileExistenceTests:
  - name: unrar
    path: /usr/bin/unrar
    shouldExist: true
