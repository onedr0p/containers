{
  "alt-speed-down": ${TRANSMISSION__ALT_SPEED_DOWN:-50},
  "alt-speed-enabled": ${TRANSMISSION__ALT_SPEED_ENABLED:-false},
  "alt-speed-time-begin": ${TRANSMISSION__ALT_SPEED_TIME_BEGIN:-540},
  "alt-speed-time-day": ${TRANSMISSION__ALT_SPEED_TIME_DAY:-127},
  "alt-speed-time-enabled": ${TRANSMISSION__ALT_SPEED_TIME_ENABLED:-false},
  "alt-speed-time-end": ${TRANSMISSION__ALT_SPEED_TIME_END:-1020},
  "alt-speed-up": ${TRANSMISSION__ALT_SPEED_UP:-50},
  "bind-address-ipv4": "${TRANSMISSION__BIND_ADDRESS_IPV4:-0.0.0.0}",
  "bind-address-ipv6": "${TRANSMISSION__BIND_ADDRESS_IPV6:-::}",
  "blocklist-enabled": ${TRANSMISSION__BLOCKLIST_ENABLED:-true},
  "blocklist-url": "${TRANSMISSION__BLOCKLIST_URL:-http://john.bitsurge.net/public/biglist.p2p.gz}",
  "cache-size-mb": ${TRANSMISSION__CACHE_SIZE_MB:-4},
  "dht-enabled": ${TRANSMISSION__DHT_ENABLED:-true},
  "download-dir": "${TRANSMISSION__DOWNLOAD_DIR:-/downloads/complete}",
  "download-queue-enabled": ${TRANSMISSION__DOWNLOAD_QUEUE_ENABLED:-true},
  "download-queue-size": ${TRANSMISSION__DOWNLOAD_QUEUE_SIZE:-5},
  "encryption": ${TRANSMISSION__ENCRYPTION:-1},
  "idle-seeding-limit": ${TRANSMISSION__IDLE_SEEDING_LIMIT:-30},
  "idle-seeding-limit-enabled": ${TRANSMISSION__IDLE_SEEDING_LIMIT_ENABLED:-false},
  "incomplete-dir": "${TRANSMISSION__INCOMPLETE_DIR:-/downloads/incomplete}",
  "incomplete-dir-enabled": ${TRANSMISSION__INCOMPLETE_DIR_ENABLED:-true},
  "lpd-enabled": ${TRANSMISSION__LPD_ENABLED:-false},
  "message-level": ${TRANSMISSION__MESSAGE_LEVEL:-2},
  "peer-congestion-algorithm": "${TRANSMISSION__PEER_CONGESTION_ALGORITHM}",
  "peer-id-ttl-hours": ${TRANSMISSION__PEER_ID_TTL_HOURS:-6},
  "peer-limit-global": ${TRANSMISSION__PEER_LIMIT_GLOBAL:-200},
  "peer-limit-per-torrent": ${TRANSMISSION__PEER_LIMIT_PER_TORRENT:-50},
  "peer-port": ${TRANSMISSION__PEER_PORT:-51413},
  "peer-port-random-high": ${TRANSMISSION__PEER_PORT_RANDOM_HIGH:-65535},
  "peer-port-random-low": ${TRANSMISSION__PEER_PORT_RANDOM_LOW:-49152},
  "peer-port-random-on-start": ${TRANSMISSION__PEER_PORT_RANDOM_ON_START:-false},
  "peer-socket-tos": "${TRANSMISSION__PEER_SOCKET_TOS:-default}",
  "pex-enabled": ${TRANSMISSION__PEX_ENABLED:-true},
  "port-forwarding-enabled": ${TRANSMISSION__PORT_FORWARDING_ENABLED:-false},
  "preallocation": ${TRANSMISSION__PREALLOCATION:-1},
  "prefetch-enabled": ${TRANSMISSION__PREFETCH_ENABLED:-true},
  "queue-stalled-enabled": ${TRANSMISSION__QUEUE_STALLED_ENABLED:-true},
  "queue-stalled-minutes": ${TRANSMISSION__QUEUE_STALLED_MINUTES:-30},
  "ratio-limit": ${TRANSMISSION__RATIO_LIMIT:-2},
  "ratio-limit-enabled": ${TRANSMISSION__RATIO_LIMIT_ENABLED:-false},
  "rename-partial-files": ${TRANSMISSION__RENAME_PARTIAL_FILES:-true},
  "rpc-authentication-required": ${TRANSMISSION__RPC_AUTHENTICATION_REQUIRED:-false},
  "rpc-bind-address": "${TRANSMISSION__RPC_BIND_ADDRESS:-0.0.0.0}",
  "rpc-enabled": ${TRANSMISSION__RPC_ENABLED:-true},
  "rpc-host-whitelist": "${TRANSMISSION__RPC_HOST_WHITELIST}",
  "rpc-host-whitelist-enabled": ${TRANSMISSION__RPC_HOST_WHITELIST_ENABLED:-false},
  "rpc-password": "${TRANSMISSION__RPC_PASSWORD}",
  "rpc-port": ${TRANSMISSION__RPC_PORT:-9091},
  "rpc-url": "${TRANSMISSION__RPC_URL:-/transmission/}",
  "rpc-username": "${TRANSMISSION__RPC_USERNAME}",
  "rpc-whitelist": "${TRANSMISSION__RPC_WHITELIST}",
  "rpc-whitelist-enabled": ${TRANSMISSION__RPC_WHITELIST_ENABLED:-false},
  "scrape-paused-torrents-enabled": ${TRANSMISSION__SCRAPE_PAUSED_TORRENTS_ENABLED:-true},
  "script-torrent-done-enabled": ${TRANSMISSION__SCRIPT_TORRENT_DONE_ENABLED:-false},
  "script-torrent-done-filename": "${TRANSMISSION__SCRIPT_TORRENT_DONE_FILENAME}",
  "seed-queue-enabled": ${TRANSMISSION__SEED_QUEUE_ENABLED:-false},
  "seed-queue-size": ${TRANSMISSION__SEED_QUEUE_SIZE:-10},
  "speed-limit-down": ${TRANSMISSION__SPEED_LIMIT_DOWN:-100},
  "speed-limit-down-enabled": ${TRANSMISSION__SPEED_LIMIT_DOWN_ENABLED:-false},
  "speed-limit-up": ${TRANSMISSION__SPEED_LIMIT_UP:-100},
  "speed-limit-up-enabled": ${TRANSMISSION__SPEED_LIMIT_UP_ENABLED:-false},
  "start-added-torrents": ${TRANSMISSION__START_ADDED_TORRENTS:-true},
  "trash-original-torrent-files": ${TRANSMISSION__TRASH_ORIGINAL_TORRENT_FILES:-false},
  "umask": ${TRANSMISSION__UMASK:-2},
  "upload-slots-per-torrent": ${TRANSMISSION__UPLOAD_SLOTS_PER_TORRENT:-14},
  "utp-enabled": ${TRANSMISSION__UTP_ENABLED:-true},
  "watch-dir": "${TRANSMISSION__WATCH_DIR:-/watch}",
  "watch-dir-enabled": ${TRANSMISSION__WATCH_DIR_ENABLED:-false},
  "watch-dir-force-generic": ${TRANSMISSION__WATCH_FORCE_GENERIC:-false}
}
